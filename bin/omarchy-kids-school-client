#!/bin/bash
# omarchy:summary=Talk to the parent school backend (JSON)
# omarchy:hidden=true

exec python3 -c 'import os, sys; sys.path.insert(0, os.environ["OMARCHY_PATH"] + "/lib/parent"); from omarchy_kids.core import cli; cli.SCOPE = "school"; raise SystemExit(cli.main())' "$@"
