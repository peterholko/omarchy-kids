#!/bin/bash
# omarchy:summary=Connect Pawberry to parent practice limits
# omarchy:hidden=true

exec python3 -c 'import os, sys; sys.path.insert(0, os.environ["OMARCHY_PATH"] + "/lib/parent"); from omarchy_kids.pawberry.client import main; raise SystemExit(main())' "$@"
